using System;
using UnityEngine;

public class PrintToConsole : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Bro, It's a hard pass!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
